version: "3.7"

services:
  # TODO: Dotnet APP Server
  postgres-db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
      POSTGRES_DB: app
    ports:
      - "5432:5432"
      